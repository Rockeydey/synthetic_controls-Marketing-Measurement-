# Want to start new project with git using existing project in git repository?
git clone <repo_url>
# File > Open Folder > Select the folder you just cloned
git pull origin <branch_name> # Want to update your local repository with changes from the remote repository? Use the following command to pull the latest changes



Quick commands:
git add . && git commit -m "Added structure" && git push origin dev