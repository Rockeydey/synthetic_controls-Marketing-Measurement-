{
  "local_version": "0.0.1",
  "updated_on": "2026-06-29",
  "parameters": {
    "combined_data_path": {
      "type": "string",
      "required": true,
      "default": "data/combined_data.csv",
      "description": "The path to the combined data file"
    },
    "output_path": {
      "type": "string",
      "required": true,
      "default": "outputs",
      "description": "Save outputs"
    }
  }
}